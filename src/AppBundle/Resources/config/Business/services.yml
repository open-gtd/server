parameters:
    entity.user: AppBundle\DataAccess\Contract\Entities\User

services:
    business.user.list:
        class: AppBundle\Business\UserList
        arguments:
            userRepository: '@repository.user'

    business.user.get:
        class: AppBundle\Business\GetUser
        arguments:
            userRepository: '@repository.user'